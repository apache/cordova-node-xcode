language: node_js
sudo: false

git:
  depth: 10

node_js:
  - 6
  - 8
  - 10

install:
  - nvm --version
  - node --version
  - npm --version
  - npm install
  - node --version
  - npm --version

script:
  - npm test
